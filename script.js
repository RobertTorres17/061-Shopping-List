// Declare a variable for the customer name
     var customername = ("Eric");
    $(".name").append(customername);
// Give it a value


// Select all elements with class "name", and use the .append() action to add the variable to elements with class "name"


// Change the variable to hold a new value
